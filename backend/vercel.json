{
    "version": 2,
    "buildCommand": "npm install && npm run build",
    "outputDirectory": "api",
    "functions": [
        {
            "path": "api/index.js",
            "memory": 1024,
            "maxDuration": 60
        }
    ]
}